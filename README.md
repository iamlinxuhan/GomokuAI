# 🎮 五子棋 AI (Gomoku AI)

基于 **PyQt5** 的五子棋人机对弈程序。AI 引擎为**位棋盘 + 全增量评估 + Negamax/PVS + 置换表 + 静止搜索 + VCF 连续冲四**，纯 CPU 运行。支持五种难度，界面为冷调设计系统配暖木棋盘，右侧面板实时画出 AI 评分与棋面胜率曲线。

搜索核心用 **C++17** 重写（同一套算法逐字对齐），通过本地 TCP 暴露给界面，
实测 nps 从约 48k 提到 **0.6–3.3M（约 20–30 倍）**。Python 那份实现完整保留为
`engine_local.py`，既是 C++ 不可用时的兜底，也是 A/B 对照的参照物。

![Python](https://img.shields.io/badge/Python-3.11%20%7C%203.13-blue)
![PyQt5](https://img.shields.io/badge/PyQt5-5.x-green)
![NumPy](https://img.shields.io/badge/NumPy-✓-orange)
![Version](https://img.shields.io/badge/version-3.0.3-brightgreen)

---

## ✨ 功能特性

### 🤖 AI 算法

| 技术 | 说明 |
|------|------|
| **位棋盘** | 361 位整数位图 + 全增量状态（候选集/邻居计数/哈希/威胁聚合），`make`/`unmake` 严格对称，支持乱序撤销 |
| **棋型判定** | 按**集合语义**定义：`F(S) = {空点 e : 在 e 落子即成五}`，`\|F\|≥2` 即活四、`\|F\|=1` 即冲四。数的是成五点的**个数**，不是"活四有几个"这类需要先解释清楚的中间量 |
| **零和评估** | `evaluate(局面, 黑) == -evaluate(局面, 白)`，增量维护、无不对称系数 |
| **Negamax + PVS** | Principal Variation Search，零窗口快速剪枝 |
| **置换表** | 定长数组 + 年龄淘汰；杀棋分按局面归一化存储（存 `value+ply`、取 `value-ply`）。4/5 档用**每桶 2 路的双路桶**（48 MiB 常驻） |
| **静止搜索** | 叶节点只搜强制着法，消除"评估被地平线截断"造成的误判 |
| **VCF 连续冲四** | 专找"每一步都在冲四"的强制序列；**三态返回**（胜 / 无胜 / 算不完），防守侧走 AND 语义。**五档全开** —— 低档的区分度由深度上限承担，不靠让初级失明 |
| **走法排序** | 历史启发 + 杀手着法 + 成五点优先 |
| **时间控制** | 硬时间上限 + 迭代加深 + 志向窗口 + 协作式取消（思考中途重开可立即中断） |
| **开局库** | 离线把最强配置在开局前四手的最优着法固化成表（41 项，覆盖盘上 ≤3 子）；命中即零延迟返回，表外一律回落搜索。空盘仍走天元，不经表 |
| **增强搜索** | LMR + 强制着法延伸 + 双路桶置换表 + 迭代预算预估，**只对高级与宗师开启** |

### 🎨 UI 设计

- **统一设计系统**：颜色/字号/间距/圆角各有单一来源（`theme.py`），全应用共用一套 QSS；界面里不出现硬编码颜色字面量（`tests/test_no_literal_colors.py` 守着）
- **冷调科技 + 暖木棋盘**：界面 chrome 走冷青，棋盘保持暖木 —— 棋盘是全局唯一的暖色焦点。木纹程序化生成，棋子为带镜面高光与接触投影的 sprite
- **四态齐全的控件**：每个按钮都生成 hover / pressed / focus / **disabled** 四态
- **右侧面板**：回合/难度/状态/悔棋次数/步数实时统计，下方两张图表
  - **AI 评分**（调试用）：每手分值折线，纵轴对数（symlog）且数量级只增不减。**实心点 = 搜索结果（有深度，是证据），空心点 = 静态估值**；下方一行等宽读数是搜索参数（深度/节点数/每秒节点数/耗时）
  - **棋面胜率（估计）**：由引擎分值换算的单调曲线，纵轴固定 0–100% 不自适应。**这是引擎模型下的估计值，不是统计标定的概率** —— 50% 只是"引擎认为均势"，只有搜索证明的杀棋才显示 100%/0%
  - **引擎 / 当前 TCP 端口**（诊断用）：前者说"这一步用了谁"（`C++` / `Python (本地)` / `开局库`），后者说"连在哪一格"
- **AI 异步计算**：QThread 线程化，UI 零卡顿

---

## 📦 快速开始

### 运行方式一：直接下载 Release

从 [Releases](https://github.com/iamlinxuhan/GomokuAI/releases) 下载对应平台的包（无需安装 Python）。Release 上只有 Linux，四个架构各一对。

文件名是四格：`GomokuAI_Linux_<家族>_<位数>_<run|setup>.<后缀>`。
`run` 是免安装的裸可执行文件（**没有后缀**），`setup` 是安装包。

| 架构 | 安装包（推荐） | 免安装裸文件 | 装法 |
|---|---|---|---|
| x86_64 | `GomokuAI_Linux_AMD_x86_64_setup.deb` | `GomokuAI_Linux_AMD_x86_64_run` | `sudo dpkg -i` → 菜单里的「五子棋AI」 |
| x86 32 位 | `GomokuAI_Linux_AMD_x86_32_setup.deb` | `GomokuAI_Linux_AMD_x86_32_run` | 同上 |
| arm64 | `GomokuAI_Linux_ARM_arm64_setup.tar.gz` | `GomokuAI_Linux_ARM_arm64_run` | 解包后 `sudo ./install.sh` |
| arm 32 位 | `GomokuAI_Linux_ARM_arm32_setup.tar.gz` | `GomokuAI_Linux_ARM_arm32_run` | 同上 |

**家族名 `AMD` 覆盖整个 x86 家族**（Intel、AMD、虚拟机都下这一份），`ARM`
覆盖整个 ARM 家族；**位数那两格才是关键** —— `AMD_x86_64` 与 `AMD_x86_32`
差一位，下错就是一句 `Exec format error`。用 `uname -m` 对照：
`x86_64` → `AMD_x86_64`，`i686` → `AMD_x86_32`，`aarch64` → `ARM_arm64`，
`armv7l` / `armv6l` → `ARM_arm32`。

不确定装哪个就用安装包：它会在 `Depends:` 里声明图形库与 CJK 字体候选链，
装完菜单里就有入口。裸文件声明不了依赖 —— 缺什么就报什么。裸文件下载后要
自己给可执行位（Release 资产不携带文件权限）：

```bash
chmod +x GomokuAI_Linux_AMD_x86_64_run
./GomokuAI_Linux_AMD_x86_64_run
```

**都是纯 CPU 运行，不需要显卡驱动，也不需要安装 PyTorch/CUDA。** 打包版不在
硬盘上留任何文件（不写日志、不写配置、不建缓存目录）；需要日志时用环境变量
指个目录：`GOMOKU_AI_LOGDIR=/tmp/gomoku ./GomokuAI_Linux_AMD_x86_64_run`。

**没有 Windows 版。** 这个项目只在 Linux 上开发与验证过，Windows 侧的配方
没有实测过，因此没有进 Release —— 把没验证过的产物放上去等于让用户当测试。

### 运行方式二：从源码运行

```bash
git clone https://github.com/iamlinxuhan/GomokuAI.git
cd GomokuAI
pip install -r requirements.txt      # Python >= 3.11
python main.py
```

---

## 🎯 游戏规则

1. 标准五子棋规则，19×19 棋盘
2. 黑棋先手，任意一方在 横/纵/斜 方向 **先连成五子** 者获胜
3. 双方交替落子，不可重复落子

---

## ⚙️ C++ 计算核心

算法不是新写的：常量表、棋型分类器、搜索结构、VCF 三态语义都与
`engine_local.py` 逐一对应，`cpp/src/*.h` 的注释里逐条记着"这几行**不能顺手
改好**"的历史 bug。

### 构建

```bash
cmake -S cpp -B cpp/build -DCMAKE_BUILD_TYPE=Release
cmake --build cpp/build -j
./cpp/build/gomoku_engine --selftest        # 进程内自检，返回 0 即通过
```

**零第三方依赖**：只用标准库 + 平台 socket（平台差异条件编译收敛在
`tcp_server.cpp` 一个文件里）。每次构建后会自动跑一次 `--verify-tables` ——
112 条线的编号差一位是这个移植里唯一会**静默出错**的地方（症状是"分数略偏"，
不报错），必须让它在构建阶段就暴露。

### 架构：两个引擎，一个门面

```
┌─ Python (UI, PyQt5) ────────────┐        ┌─ C++17 ────────────────┐
│  main.py                        │        │  gomoku_engine          │
│    └ AIWorker(QThread)          │  TCP   │   ├ Board (位棋盘)      │
│         └ engine.ai_move() ─────┼───────►│   ├ evaluate (全增量)   │
│              │                  │ JSON行 │   ├ search (PVS+TT+静止)│
│              ├─ 成功 → C++ 结果  │◄───────┤   └ vcf (连续冲四)      │
│              └─ 失败 → 静默降级 ↓│        │  单客户端 / 可取消      │
│  engine_local.py  (原引擎)      │        └────────────────────────┘
│    兜底 + 全部轻量函数原本地执行  │
└─────────────────────────────────┘
```

**只有 `ai_move` 走 TCP。** `check_win` / `win_line` / `evaluate` / `opening_move`
留在本地，因为它们**全在 UI 主线程同步执行**（每落一子都要跑一遍）。挪到 TCP 上
等于让界面每手同步阻塞一次网络往返，与"UI 零卡顿"直接冲突。

**难度参数由 Python 权威下发。** 每次请求带 `time/max_depth/vcf_budget/qply`
与入门档的偏置系数，C++ 侧不写死难度表。好处是校准参数不必重编译，而且"难度"
这个概念留在 UI 层 —— 与"PvP 时 AI 只是其中一个玩家"的抽象一致。

**产物 `gomoku_engine` 是服务端，界面不需要你去启动它**：`engine.py` 惰性连接，
连不上就自己拉起，退出时经 `atexit` 停掉。固定端口会撞上残留的旧版本进程，
所以端口是一个**池子**（`config.PORT_POOL`，15 个不常用端口），逐个试，每格
都**先过 `hello` 校验**才执行搜索 —— "连得上"不等于"是我们的引擎"。只监听回环。

**降级是静默的，且不出卖棋局。** 服务端没编译、启动失败、崩溃、被另一个窗口
顶掉、15 格全不可用 —— 一律翻译成"这次用本地引擎"，棋局继续，**不弹窗、不抛
异常**，只在 stderr 留一行原因，并让面板上「引擎」那一行转为 `Python (本地)`。
**开局库不算降级**：那一手既没搜索也没走 TCP，但是**设计**，所以报 `开局库`
（报成 `Python (本地)` 会让整局第一步就报出一个假警）。档位超出本地表时
（本地只有 3 档）4/5 落到**最强的那一档**，而不是报错。

### 两个引擎的刻度必须一致

C++ 移植沿用 Python 的评分刻度，一个常量都没改。`analysis.py` 的胜率锚点直接
钉在 `LV_THREE_LIVE`、`BONUS_DOUBLE_FOUR`、`STATIC_MAX` 上
（`tests/test_analysis.py::test_anchors_are_engine_constants` 逐个校验），刻度
一致时右侧两张图天然正确；刻度错了，图会**静默**画错。客户端因此在握手时校验
`board_size` / `win_score` / `static_max` 三项，对不上就换端口自己拉起。

### 一个反直觉的坑：`_VCF_NODE_CAP`

Python 版 `_VCF_NODE_CAP = 120000` 约合 2.4 秒，远大于最高档 1.5 秒的 VCF
预算，所以基本不触发。**C++ 下 12 万节点只要约 0.05 秒，这个上限会在每一档
都触发** —— 大量本可证明的杀棋被判成 `EXHAUSTED`（"算不完"，即不表态），
**引擎在 VCF 上反而比 Python 版更弱**。这与"C++ 更强"的直觉相反，是这类
移植最容易漏掉的一处。现在它提到 `VCF_NODE_CAP` 并随请求下发。

### 实测吞吐

| 引擎 | nps 中位（5 个固定中局） |
|---|---|
| `engine_local.py`（Python + numpy） | 47,000 – 50,000 |
| `gomoku_engine`（C++17） | 0.6M – 3.3M |

约 **20–30 倍**。注意 nps 随档位**下降**（档位 2 约 430k、档位 5 约 940k）不是
退化：高档位的 qply 更大，静止搜索节点占比升到 40%–90%，而 qnodes 比主搜索
节点便宜得多，混进同一个分母会把 nps 拉下来。

---

## 🎛️ 难度说明

五档参数就是 `engine.py` 的 `DIFFICULTY`，单位与数值可直接对照：

| 难度 | 每步时限 | 深度上限 | 静止搜索层数 | VCF 预算 | 增强搜索 | 进攻偏置（容差带） |
|------|---------|---------|------------|---------|---------|------------------|
| **入门** | 0.5 s | 2 | 2 | 0 s | — | 1.5 / 0.5，带 0 |
| **初级** | 3 s | 4 | 4 | 0.3 s | — | — |
| **中级** | 7 s | 10 | 8 | 0.5 s | — | — |
| **高级** | 15 s | 24 | 10 | 1.5 s | ✅ | — |
| **宗师** | 20 s | 24 | 12 | 3.0 s | ✅ | — |

- **"深度上限"是上限，不是承诺。** 实际到达的层数由迭代加深在时限内决定 ——
  开局几手会说走就走（评估认为无可搜之物），中盘才会走满时限。
- **VCF 的时间算在上述时限之内**，不是额外开销。
- **五档都开 VCF。** 初级档曾经关掉它，那是个错误：关掉之后初级既算不出
  自己的冲四链，也看不见对手的 —— 那不是"难度低"，是**失明**。真实败局里
  人类用一条 11 手 VCF 链取胜，而初级全程没有任何机制能看见它。现在低档的
  区分度由**深度上限**承担，不靠缺失子系统。
- **只有入门档带"根节点进攻偏置"**，且它只在根节点生效，绝不注入
  `evaluate()` —— 让评估本身不对称会破坏它的零和性，而零和性是搜索正确性的
  前提。机制是一条**容差带**：分值与最好那一手相差在带内（入门档的带是 0）
  的着法仍算候选，带内改按 `1.5×己方棋型 − 0.5×对手威胁` 选一手；带外一分
  不让，杀棋结论（`is_mate`）不参与重排。所以它**只在分值完全相同的一手之间
  重排**，一分不牺牲。
  初级与中级曾经也带过一条 3000 的容差带（为了"更进取"），**量完撤掉了**：
  同档自我对打，偏置开的一方在初级 25–35%、在中级 **0%（0—8）**。连"只在同
  分之间重排"都输 6—14，说明真因不是带宽，而是**这个判据与搜索的判断相反** ——
  任何成四的一手都把"己方棋型"抬到约 100000，压倒性地赢得重排；而在 4—6 层
  的深度下"造一个对手随手应住的冲四"并不等于进展。**"更进取"和"更弱"必须分开
  量了再谈 —— 这次量出来它们是同一件事。**

**实测（`python tools/bench.py --engine cpp --level N`，5 个固定局面）**。
注意"深度上限"是配置值、"深度中位"是实际到达值：

| 档 | 墙钟中位 / 最大 | 深度中位 / 最大 | nps 中位 | 时间合规 |
|---|---|---|---|---|
| 入门 | 44 ms / 65 ms | 2 / 2 | 12,504 | 5/5 |
| 初级 | 76 ms / 260 ms | 4 / 4 | 488,899 | 5/5 |
| 中级 | 5992 ms / 5996 ms | 5 / 6 | 1,279,829 | 5/5 |
| 高级 | 8640 ms / 12 792 ms | 6 / **9** | 977,729 | 5/5 |
| 宗师 | 9216 ms / 17 048 ms | 6 / **9** | 894,351 | 5/5 |

**入门与初级远远没跑满时限**（44 ms / 76 ms）：它们的 `max_depth` 是 2 / 4，
上限先于时钟生效 —— 这是刻意的。**高级与宗师在这 5 个局面上深度相同**：这 5
个局面里有好几个是**已经被判定的**（例如 `mid_10stones` 的轮走方已经必败），
本就该秒回。**阶梯要在窄树上才拉得开**，两档的真实分野是 15 s vs 20 s 与
`vcf_budget` 1.5 s vs 3.0 s，在宽度大的中盘上才体现出来。这张表读的是"这 5 个
局面有多快解决"，不是"引擎总是这么快"。

**开局五档共用同一张开局库。** 这是一个刻意的取舍：入门/初级的开局头几手
现在也是高手级的着法，**梯度在开局这几手上是窄的**，换来的是"每一档的开局都
不再是硬搜出来的第一手"。另外**高级/宗师不带进攻偏置**还有一条独立理由：
开局库是用不带偏置的宗师建出来的，给它们加偏置会让库里存着的着法与"现搜一手"
给出的不一致。

**中级的 7 秒是被一个具体局面定出来的，不是拍的。** 一条真实败局
（中级执白、人类第 31 手 J8 胜）的胜负手在第 4 手：盘面只有三颗子时，**第 4
层选 K9，第 5 层选 K7/K11** —— 相邻两层给出胜负相反的结论。用 5.0 s 预算
重复搜这一手会给出**两种答案**，比例随机器多忙而变（起 6 个满载进程后 4/4
全走败着）；7.0 s 在 4/4 稳定给出正解。**这是把地平线推远，不是治好了** ——
任何固定预算都有地平线，这条时限只保证"这一个局面看得见"。

---

## ⚡ 增强搜索（只对高级与宗师开启）

`SearchConfig` 有三个字段承担这件事，**默认全 0**：`enhanced`（总开关）、
`lmr`、`extend`（后两个只为隔离测量）。

**默认 0 是这套设计的关键。** `enhanced == 0` 时**每一条代码路径与从前逐位
相同** —— 于是 `--selftest` 里钉死的精确着法与 `WIN_SCORE-2` 那个 ply 编码
一字不改地通过，初/中两档与 Python 参考实现 `engine_local` 的逐字一致继续
成立，LMR 不可能悄悄漏进低档。

### 同一时限下真的多搜了层

五个固定局面，**预算一分没涨**（L4 = 15 s），只切 `enhanced`；`lmr` 与
`extend` 各自可单独打开，就是为了回答"到底是谁干的活"：

| 局面 | 关闭 | 只开 LMR | 只开延伸 | 两个都开 |
|---|---|---|---|---|
| `mid_10stones` | 1 | 1 | 1 | 1 |
| `mid_clash` | 5 | 5 | 5 | 5 |
| `spread_wide` | 5 | **6** | 5 | **6** |
| `dense_threat` | 6 | **9** | **8** | **9** |
| `sparse_early` | 6 | **8** | 6 | **8** |
| **中位 / 最大** | **5 / 6** | **6 / 9** | 5 / 8 | **6 / 9** |

**LMR 是主杠杆**：它单独就拿到了与"两个都开"完全相同的深度。**延伸单独在开阔
局面上不动中位**，只在威胁密集的 `dense_threat` 上贡献 6 → 8 —— 这与它的设计
意图一致（它延伸的是"自己造出了必须应的威胁"那种着法，威胁稀的局面里根本触发
不了几次）。**两个都开从不比单独任何一个差**，所以生产配置取 `both`。

`dense_threat` 上 11.1 秒就完成到 9 层，**没跑满 12.8 秒** —— 这不是把时间堆
出来的。同一局面在 `--selftest` 的中局探针里从
`dep=6 / 189.2 万节点 / 1700 ms` 变成 `dep=7 / 145.0 万节点 / 1074 ms`：
**层数更深、节点更少、耗时更短**。

### A/B 胜率：判据必须落在"处理真的生效"的预算上

`tools/ab_enhance.py` 是配对 A/B（同开局、交换先后手各一遍）。把每手预算压低
再跑是有讲究的 —— 先量"处理是否生效"：

| 每手预算 | 关闭深度 | 开启深度 | 有深度差？ |
|---|---|---|---|
| 1.5 s | 中位 5，最大 6 | 中位 5，最大 6 | **没有** |
| 6 s | 中位 5，最大 6 | 中位 6，最大 8 | 有 |
| 15 s | 中位 5，最大 6 | 中位 6，最大 9 | 有 |

道理不复杂：LMR 在 `depth < 3` 时整段不生效，延伸要 `depth >= 3`，而 1.5 s
只够搜到 4–6 层。**在 1.5 s 上两个臂走的是同一手棋，胜率当然只能是 50 %。**
所以在处理真正生效的 6 s 上测（30 局，配对）：

| 总局数 | 增强侧胜 / 负 | 胜率 | 总体 p | 配对 McNemar p | 执黑 Fisher p |
|---|---|---|---|---|---|
| 30（15 对） | **21 / 9** | **70.0 %** | 单侧 0.021 | 单侧 0.035 | 双侧 0.035 |

**最有信息量的是执黑那一列**：五子棋是先手优势极大的棋，30 局里先手方赢了
22 局。增强臂**执黑 14 胜 1 负（93 %）**，而基线执黑只有 8 胜 7 负 —— 差距
几乎全部来自"拿到先手时能不能兑现"。执白时增强臂 7/15（47 %），与基线大致
持平：白方的劣势不是靠多搜两层就能翻过来的。

**一条限制要说清**：6 s 是"处理生效"的预算，生产档是 15 s / 20 s。**不该把
70 % 外推成"生产档也赢 70 %"** —— 能做的最强陈述是"在增强真正生效的预算上，
它明显更强"。完整证据链是：深度（同预算中位 5→6、最大 6→9，且节点更少、耗时
更短）→ 胜率（70 %，p ≈ 0.02–0.035）→ 解题率不降（L4/L5 在 `tools/positions.py`
上仍是 8/8，8 道题的着法与分值逐字未变）。

### 两条被量化出来的坑

- **迭代预算预估的早退过度保守。** "下一轮值不值得开"那条判据乘的是一个噪声
  很大的分支因子估计，1.5 s 预算实测只花掉 26% 就收工、还比基线浅一层。关键
  在于"试一轮"与"直接早退"的代价是**不对称**的：试了没搜完 = 同样的深度 +
  多花的时间；直接早退 = 同样的深度 + 省下的时间。**试一次是免费的期权**，
  所以加了第二道闸：预算至少花掉一半，早退才被允许。**这条值得单独记，因为
  它的症状与"算法变笨"无法区分** —— 增强臂在短预算下既没更慢也没出错，只是
  "提前满意了"。
- **双路桶查表把一半命中扔掉了。** TT 从"直接映射"改成"每桶 2 路"时两套几何
  共用同一块数组，而 `lookup()` 里"只有第 0 路命中"那条分支从不覆盖下标指针
  —— 于是命中了、却拿着旧下标的条目去比 key，比不过就当成未命中返回。**命中
  被静默丢弃**，引擎行为没有任何异常，只是变笨了（TT 命中率 1.5%–5.9% →
  **0.0%–0.2%**）。`--selftest` 因此加了一条 `ttHitRate > 0.005` 的护栏 ——
  **门槛不能写成 `> 0`**，那对"命中率掉到 0.1%"这种几乎全丢的情况完全无感。

---

## 📖 开局库

离线把最强配置在开局前四手的最优着法固化成一张表，运行时查表即取。生成物是
`GomokuAI/opening_book.py`（**由 `tools/build_book.py` 生成，不要手工编辑**），
41 个表项，覆盖盘上 ≤3 子的局面。

```bash
python tools/build_book.py               # 默认 20 s/节点，中断可续跑
python tools/build_book.py --port 8899   # 游戏开着时必须换端口
```

**树只在对手的轮次上分叉。** 我们自己的轮次只存一手（实战里我们**总是**走它，
为"我们自己不下自己最优着法"的世界建库是白花钱）；对手的轮次展开 4 手 ——
只有对手会偏离，而每种偏离都要有答案。于是整棵树只有 `1 + 8 + 8×4 = 41` 个
节点，第 2 手的 8 个分支就是 `tools/selfplay.py` 里那份 `OPENING_SECOND_MOVES`。
**分支里必须含"对手自己那一手"这个位置** —— 库按 `(局面, 走子方)` 分区，黑方
走出它的最优着法之后轮白方，那是一个**新键**，而且正是概率最高的那个键。

**只有 Python 一份表，C++ 侧没有镜像。** 走 TCP 的只有 `ai_move` 的 `compute`，
而库是在 `engine.py` 的 `ai_move` 里、**在发起请求之前**就命中的 —— C++ 的
`openingMove()` 根本不在生成着法的路径上。于是"镜像一份"的唯一效果就是多出
一份会漂移的表：两侧各有一套 Zobrist 哈希，一旦分叉，C++ 会**静默地**查不到
任何条目（表现为"库好像没生效"）。**不写第二份表，就没有分叉这回事。**

**空盘仍然走天元，不进表。** `opening_move` 的"空盘 → 天元"是**钉死的规则**，
`main.py` 与文档都依赖它。⚠️ **闸门必须基于"查表命中"，不能写成"盘上子少"**
—— 写成后者会把那条"紧贴对手第一子按固定偏移取点"的老规则一并激活，那不是
加强，是**降智**。

**库只覆盖标准开局集 —— 走偏了就是"回落搜索"，不是"走错"。** 对手只要走出那
4 个分支之外的一手，局面就不再是任何库项的键 → 查不到 → **回落正规搜索，与
没有这个库时逐字相同**。库没有"近似匹配"这种退路（哈希是精确比较），所以
库里哪怕存着一手坏棋，它也只能在**它自己那些局面**上生效，不会外溢。

**分值如实回传。** 表里存的是建库时的**真实评估**（走子方视角），命中时原样
上报，`nodes` 为 0 而 `best_val` 非 0。代价是入门/初级档在开局头几手会在胜率
图上显示出高手级读数 —— 库里存的就是高手级评估，填 0 反而是撒谎。

**面板上那一行报「开局库」。** 「引擎」那一行有三个取值，对应三种**真的不同**
的执行路径：`C++`（走了服务端）、`Python (本地)`（降级）、`开局库`（查表，零
延迟）。第三条单独列出来是必要的 —— 报成 `C++` 是假话（它根本没连），报成
`Python (本地)` 更糟：那是"降级"的同义词。注意 AI 先手的第一着走的是
`opening_move`、**不经过 `ai_move`**，指示器在那条路上是断的，所以有
`engine.note_book()` 这个公开入口；两条路报同一个值由
`tests/test_opening_book.py` 直接断言。

---

## 🧠 引擎设计

### 杀棋分与静态分分带

静态分钳在 ±`STATIC_MAX`，杀棋分在 `WIN_SCORE - MAX_PLY` 以上，中间留 128
的真空带 —— 因此"这是必杀"与"这个局面看起来不错"在数值上不会混淆，
`is_mate()` 的判定是可靠的而非推测。

旧引擎的将杀分（`-10000000 - depth`）与它自己的复合静态分**量程重叠** ——
拼命模式的攻防加权可以到 ±3.45e7，比将杀分还大。于是"我三步后必死"与"这局面
挺糟糕"在数值上分不开，界面看到的只是一个很大的负数。差别的关键不在快慢，
而在**这个数字能不能被读懂**。

### 时间是硬上限，深度上限是安全阀

`think` 收下难度档位后设一个**硬**截止（`t0 + time × 0.85`，留 15% 余量给回传
与 UI），迭代加深在这个截止前能跑多深就跑多深，`max_depth` 只是防止"看起来
安静、实际到处是四"的中局把低档也拖到 20 层。

**安全阀必须真的成为约束。** 初级档曾经不是：它的时间只够第 3 层，而在真实
败局的那个中局上，第 3 层给出的判断与第 4 层**方向相反**（第 3 层报"略处下风"，
第 4 层报"已经快死了"）。
`tests/test_difficulty.py::test_low_level_reaches_its_own_depth_cap` 钉着这条。

### 为什么删掉威胁响应层

旧版在搜索之前有一层约 130 行的 `_check_immediate_threat`，用七段启发式规则
抢答"现在该走哪"。它的失败方式是结构性的：**启发式判对时省下的时间，远小于
判错时输掉的棋。**

最典型的一处判据写成 `opp_win >= 2 or opp_live4 >= 2`，而实战里最常见的双杀
形态是 `opp_win == 1 且 opp_live4 >= 1` —— **恰好落在判据之外**。补丁是加一条
规则，而规则永远补不完；真正的问题是"用有限条规则去近似一个可以精确判定的
问题"。新引擎把它拆给两个能给出确定性结论的机制：静止搜索数成五点的个数
（精确），VCF 负责更长的强制序列（同样精确，且能明确区分"没有杀"与"我没算完"）。
`tests/test_threat.py` 断言的正是"**这些名字不该再存在**"—— 它们当年处理的
局面必须仍然被正确处理，但不能再靠启发式。

---

## 📁 项目结构

```
GomokuAI/
├── main.py            # 界面组装 + 游戏流程接线（不含搜索/评估逻辑）
├── engine.py          # 引擎门面：TCP 客户端 + 玩家抽象 + 静默降级（**不含算法**）
├── engine_local.py    # AI 引擎本体：位棋盘 / 评估 / 搜索 / VCF（零 Qt，仅依赖 numpy，可脱离界面单测）
├── config.py          # C++ 可执行文件的定位与连接参数（只做路径解析，不做 IO）
├── cpp/               # C++17 计算核心（零第三方依赖，CMake）
│   └── src/           # board / evaluate / search / vcf / opening / tcp_server + json_util
├── analysis.py        # 分值 -> 胜率的折算、symlog 映射、读数格式化（纯函数，零 Qt）
├── charts.py          # 面板上的两张自绘图表（折线 / 网格 / 标记点）
├── gamelog.py         # 对局日志与棋谱坐标格式
├── theme.py           # 设计系统：调色板 / 字号 / 间距 / 圆角 / QSS 生成
├── ui_kit.py          # 可复用控件原语（标题、信息行、按钮、页面骨架、科技底纹）
├── board_geometry.py  # 像素 <-> 格子换算（纯 math，零 Qt）
├── board_render.py    # 棋盘离屏渲染（木纹 / 棋子 sprite / 图层缓存）
├── tests/             # pytest：几何、增量引擎、题库、胜率折算、颜色字面量守卫
├── tools/             # bench / selfplay / positions / gui_smoke / ui_e2e / build_book / ab_enhance …
│   ├── BASELINE.md    # 各阶段实测基线与门槛
│   └── legacy_engine.py  # 旧引擎逐字快照（不得修改，作为 A/B 对照组）
├── packaging/         # Linux 四架构共用的打包配方（容器内构建）
├── installer/         # Windows 安装包脚本（Inno Setup，UTF-8 带 BOM）
├── requirements.txt      # 运行时依赖（numpy / PyQt5）
└── requirements-dev.txt  # 开发与打包依赖（含 pytest / pyinstaller）
```

---

## 🧪 测试

```bash
pip install -r requirements-dev.txt
python -m pytest -q                     # 全套 342 条
python -m pytest -q -m "not perf"       # 跳过机器速度相关的门槛（CI 用这条，321 条）
```

**关于 `perf` 标记**：少数门槛测的是"引擎有没有退化"，但读数是"每秒多少
节点""3 秒内到了第几层"—— 在更慢的机器上，退化和慢机器在数字上无法区分。
**正确性、增量一致性、时间合规、题库、VCF、棋型判定这些都不带标记**，它们在
任意机器上都该通过 —— 时间上下限由引擎自己强制，与机器快慢无关。

主要覆盖面：位棋盘连五检测与增量状态差分（`test_win` / `test_incremental`）、
棋型偏序与零和性（`test_eval`）、置换表条目类型与杀棋分归一化（`test_tt`）、
**找得到杀且认得必败**（`test_search_mate`）、**被判死刑时不许认命**
（`test_escape`）、VCF 三态与"三档都真的开 VCF"（`test_vcf`）、难度门槛与
**低档要够得到自己的深度上限**（`test_difficulty`）、取消延迟（`test_cancel`）、
胜率折算的锚点必须是 `engine` 常量（`test_analysis`）、开局库的**接线**
（`test_opening_book`）、颜色字面量守卫（`test_no_literal_colors`）。

另有 `tools/gui_smoke.py`（无头界面冒烟，测**接线**）与 `tools/ui_e2e.py`
（**五档各真下一局**，走真实点击路径）。后者的人类一方是确定性策略（首手天元），
于是**首手天元能让开局库真的被命中** —— "开局库在 UI 里生效"因此能在日志里被
验证。实测五档：

| 档 | 手数 | AI 最大搜索深度 | 开局库命中 |
|---|---|---|---|
| 入门 | 10 | 2 | 2 手 |
| 初级 | 10 | 4 | 2 手 |
| 中级 | 10 | 6 | 2 手 |
| 高级 | 10 | 8 | 2 手 |
| 宗师 | 10 | 8 | 2 手 |

**这正是要拉开的那条梯度，在真实对局里量出来了**（2 / 4 / 6 / 8）。同一张表
还顺带证实了开局库在每一档都命中（`0 ms`），而**入门档的开局库读数是 `dep=9`**
—— 库里存的是建库时的真实深度，原样上报，这是刻意保留的行为。

---

## 📝 更新日志

| 版本 | 日期 | 内容 |
|---|---|---|
| **v3.0.3** | 2026-09-26 | 难度选择页两处修正：暗色主题下强度条按主题取子色（黑子对暗色卡面只有 1.11:1）；宗师那张的强度条改为压缩盒间距而不是缩小棋子（原 191px 塞不进 136px 的卡片内区）。v3.0.2 那个 tag 没过 `test`、没有产出 Release，其内容并入本版 |
| v3.0.1 | 2026-09-25 | 端口池（固定 8888 → 15 格，每格都过 `hello` 校验）；面板新增「当前 TCP 端口」与「开局库」两个诊断取值；产物名拆成家族/位数/用途三格，`.pkg` → `.tar.gz`；发版闸门重写（原版在空目录里展开通配符，8 个资产全在却报"缺少产物"） |
| v3.0.0 | 2026-09-25 | 搜索核心换成 C++17，难度从三档扩到五档，高级/宗师开启真正的搜索裁剪（LMR + 强制着法延伸 + 双路桶置换表）；新增开局库 |
| v2.0.2 | 2026-09-24 | 中级档时限 5.0 s → 7.0 s（一条真实败局定出来的，见「难度说明」） |
| v2.0.1 | 2026-09-24 | 修掉一处评估对称性与棋谱坐标格式问题 |
| v2.0.0 | 2026-09-19 | **彻底重写**：评估与搜索层整体替换、界面重构为统一设计系统。旧的"分层 TSS 威胁响应""多线防守""拼命模式"与 GPU 分支全部删除 |
| v1.5 – v1.8.0 | — | 早期版本。旧版 README 有一篇 166 行的对局复盘，讲的是已删除的机制；全文仍在历史里：`git show d232fa7:README.md \| sed -n '236,401p'` |
